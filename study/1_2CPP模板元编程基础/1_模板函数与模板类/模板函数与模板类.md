# 模板函数与模板类

> 模板是C++实现泛型编程和编译期多态的核心机制

---

## 1. 为什么需要模板

### 1.1 代码复用的困境

在没有模板之前，如果我们想实现一个通用的交换函数，需要为每种类型单独实现：

```cpp
// 为int类型实现
void swap(int& a, int& b) {
    int temp = a;
    a = b;
    b = temp;
}

// 为double类型实现
void swap(double& a, double& b) {
    double temp = a;
    a = b;
    b = temp;
}

// 为string类型实现...
// 为自定义类型实现...
// 代码大量重复！
```

### 1.2 模板的解决方案

模板允许我们编写**类型无关**的代码，由编译器根据使用时的具体类型自动生成对应版本：

```cpp
template <typename T>
void swap(T& a, T& b) {
    T temp = a;
    a = b;
    b = temp;
}

// 使用时：
int x = 1, y = 2;
swap(x, y);         // 编译器生成 swap<int>

double a = 1.0, b = 2.0;
swap(a, b);         // 编译器生成 swap<double>
```

---

## 2. 函数模板

### 2.1 基本语法

```cpp
template <typename T>    // 或 template <class T>
返回类型 函数名(参数列表) {
    // 函数体，可以使用类型T
}
```

**完整示例：**

```cpp
template <typename T>
T max(T a, T b) {
    return (a > b) ? a : b;
}

// 调用方式
int result1 = max(10, 20);        // 自动推导：max<int>
double result2 = max(3.14, 2.71); // 自动推导：max<double>
int result3 = max<int>(10, 20);   // 显式指定类型
```

### 2.2 模板参数推导

编译器可以根据函数参数自动推导模板类型：

```cpp
template <typename T>
void print(T value) {
    std::cout << value << std::endl;
}

print(42);        // T 推导为 int
print(3.14);      // T 推导为 double
print("hello");   // T 推导为 const char*
```

**推导规则图解：**

```
调用 print(42)
      │
      ▼
实参类型: int
      │
      ▼
模板参数推导: T = int
      │
      ▼
实例化: void print<int>(int value)
```

### 2.3 多个模板参数

```cpp
template <typename T, typename U>
auto add(T a, U b) -> decltype(a + b) {
    return a + b;
}

// C++14起可以简化为：
template <typename T, typename U>
auto add(T a, U b) {
    return a + b;
}

// 使用
auto result = add(1, 2.5);  // T=int, U=double, 返回double
```

---

## 3. 类模板

### 3.1 基本语法

```cpp
template <typename T>
class 类名 {
    // 可以使用类型T作为成员类型
    T data;
public:
    T getValue() const { return data; }
    void setValue(T value) { data = value; }
};
```

**完整示例：**

```cpp
template <typename T>
class Container {
private:
    T value;
    
public:
    Container(T v) : value(v) {}
    
    T get() const { return value; }
    void set(T v) { value = v; }
};

// 使用时必须显式指定类型（C++17之前）
Container<int> intBox(42);
Container<std::string> strBox("hello");
```

### 3.2 类模板与成员函数

成员函数可以在类外定义：

```cpp
template <typename T>
class Stack {
private:
    std::vector<T> data;
    
public:
    void push(const T& value);
    T pop();
    bool empty() const;
};

// 类外定义需要重复template声明
template <typename T>
void Stack<T>::push(const T& value) {
    data.push_back(value);
}

template <typename T>
T Stack<T>::pop() {
    T top = data.back();
    data.pop_back();
    return top;
}

template <typename T>
bool Stack<T>::empty() const {
    return data.empty();
}
```

### 3.3 类模板图解

```
┌─────────────────────────────────────────────────────────────────┐
│                    类模板 Stack<T>                               │
├─────────────────────────────────────────────────────────────────┤
│  template <typename T>                                          │
│  class Stack {                                                  │
│      std::vector<T> data;      ← T是类型占位符                   │
│      void push(const T&);                                       │
│      T pop();                                                   │
│  };                                                             │
└─────────────────────────────────────────────────────────────────┘
                              │
           ┌──────────────────┼──────────────────┐
           │                  │                  │
           ▼                  ▼                  ▼
    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
    │ Stack<int>  │    │Stack<double>│    │Stack<string>│
    │             │    │             │    │             │
    │vector<int>  │    │vector<double│    │vector<string│
    │push(int&)   │    │push(double&)│    │push(string&)│
    └─────────────┘    └─────────────┘    └─────────────┘
         实例化              实例化              实例化
```

---

## 4. 非类型模板参数

### 4.1 什么是非类型参数

模板参数不仅可以是类型，还可以是**编译期常量值**：

```cpp
template <typename T, int Size>  // Size是非类型参数
class Array {
private:
    T data[Size];  // 编译期确定数组大小
    
public:
    T& operator[](int index) { return data[index]; }
    constexpr int size() const { return Size; }
};

// 使用
Array<int, 10> arr;     // 创建包含10个int的数组
Array<double, 100> big; // 创建包含100个double的数组
```

### 4.2 非类型参数的限制

非类型参数必须是**编译期常量**，支持的类型包括：
- 整型（int, long, size_t等）
- 枚举类型
- 指针和引用（指向静态存储期的对象）
- C++20起支持浮点数和字面量类类型

```cpp
template <int N>
struct Factorial {
    static constexpr int value = N * Factorial<N-1>::value;
};

template <>
struct Factorial<0> {
    static constexpr int value = 1;
};

// 使用
constexpr int fact5 = Factorial<5>::value;  // 编译期计算：120
```

---

## 5. 模板模板参数

### 5.1 概念

模板参数本身也可以是一个模板：

```cpp
template <typename T, template <typename> class Container>
class Wrapper {
    Container<T> data;
public:
    void add(const T& value) { data.push_back(value); }
};

// 使用
Wrapper<int, std::vector> w;  // Container = std::vector
```

### 5.2 实际应用

```cpp
template <typename T, 
          template <typename, typename> class Container = std::vector,
          typename Allocator = std::allocator<T>>
class Stack {
    Container<T, Allocator> data;
public:
    void push(const T& value) { data.push_back(value); }
    // ...
};

// 可以使用不同的底层容器
Stack<int> defaultStack;                    // 使用vector
Stack<int, std::deque> dequeStack;          // 使用deque
Stack<int, std::list> listStack;            // 使用list
```

---

## 6. 变参模板（Variadic Templates）

### 6.1 基本语法

C++11引入变参模板，允许接受任意数量的模板参数：

```cpp
template <typename... Args>  // Args是参数包
void print(Args... args) {   // args是函数参数包
    // ...
}
```

### 6.2 参数包展开

```cpp
// 递归展开方式
template <typename T>
void print(T value) {
    std::cout << value << std::endl;
}

template <typename T, typename... Args>
void print(T first, Args... rest) {
    std::cout << first << " ";
    print(rest...);  // 递归调用，展开参数包
}

// 使用
print(1, 2.5, "hello", 'c');
// 输出: 1 2.5 hello c
```

### 6.3 折叠表达式（C++17）

C++17提供了更简洁的参数包展开方式：

```cpp
template <typename... Args>
auto sum(Args... args) {
    return (args + ...);  // 折叠表达式：((arg1 + arg2) + arg3) + ...
}

template <typename... Args>
void printAll(Args... args) {
    (std::cout << ... << args) << std::endl;
}

// 使用
auto result = sum(1, 2, 3, 4, 5);  // 15
printAll("Hello", " ", "World");    // Hello World
```

### 6.4 折叠表达式类型

```
一元右折叠: (args op ...)      → arg1 op (arg2 op (arg3 op arg4))
一元左折叠: (... op args)      → ((arg1 op arg2) op arg3) op arg4
二元右折叠: (args op ... op init) → arg1 op (arg2 op (arg3 op init))
二元左折叠: (init op ... op args) → ((init op arg1) op arg2) op arg3
```

---

## 7. 模板实例化

### 7.1 隐式实例化

当使用模板时，编译器自动生成对应类型的代码：

```cpp
template <typename T>
T square(T x) { return x * x; }

// 使用时触发隐式实例化
int a = square(5);      // 实例化 square<int>
double b = square(3.0); // 实例化 square<double>
```

### 7.2 显式实例化

可以强制编译器在特定位置实例化模板：

```cpp
// 显式实例化定义（放在.cpp文件中）
template class Stack<int>;
template void print<double>(double);

// 显式实例化声明（告诉编译器不要在此处实例化）
extern template class Stack<int>;
```

### 7.3 实例化过程图解

```
源代码                    编译器处理                    生成代码
────────────────────────────────────────────────────────────────
template <typename T>                                  
T square(T x) {           ──────────────────────→     // 模板定义
    return x * x;                                      // 不生成代码
}                                                      

int a = square(5);        ──────────────────────→     int square(int x) {
                          实例化 square<int>               return x * x;
                                                       }

double b = square(3.0);   ──────────────────────→     double square(double x) {
                          实例化 square<double>            return x * x;
                                                       }
```

---

## 8. 与FlashAttention的关联

### 8.1 kernel_traits.h 中的模板类

FlashAttention使用模板类来定义Kernel的编译期配置：

```cpp
// 简化版本
template<int kHeadDim_, int kBlockM_, int kBlockN_, int kNWarps_, 
         bool Is_Q_in_regs_ = false, typename elem_type = cutlass::half_t>
struct Flash_fwd_kernel_traits {
    // 非类型模板参数成为编译期常量
    static constexpr int kHeadDim = kHeadDim_;
    static constexpr int kBlockM = kBlockM_;
    static constexpr int kBlockN = kBlockN_;
    static constexpr int kNWarps = kNWarps_;
    
    // 类型别名
    using Element = elem_type;
    
    // 派生的编译期常量
    static constexpr int kNThreads = kNWarps * 32;
    static constexpr int kBlockKSmem = kHeadDim % 64 == 0 ? 64 : 32;
    
    // cute Layout类型定义
    using SmemLayoutQ = /* ... */;
    using SmemLayoutK = /* ... */;
};
```

### 8.2 为什么使用模板

```
┌─────────────────────────────────────────────────────────────────┐
│                     使用模板的好处                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. 编译期确定循环边界 → 循环展开优化                             │
│     for (int i = 0; i < kBlockM; i++)  // kBlockM是编译期常量     │
│                                                                  │
│  2. 编译期确定数组大小 → 栈分配，无动态内存                        │
│     __shared__ float smem[kBlockM][kBlockN];                     │
│                                                                  │
│  3. 编译期分支消除 → 无运行时开销                                 │
│     if constexpr (Is_causal) { /* ... */ }                       │
│                                                                  │
│  4. 针对不同配置生成优化代码                                      │
│     Flash_fwd_kernel_traits<64, 128, 64, 4>  // HeadDim=64      │
│     Flash_fwd_kernel_traits<128, 128, 64, 4> // HeadDim=128     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.3 变参模板在cute中的应用

cute库大量使用变参模板来处理任意维度的张量：

```cpp
// cute中的make_tuple使用变参模板
template <typename... Ts>
auto make_tuple(Ts&&... ts) {
    return cute::tuple<std::decay_t<Ts>...>{std::forward<Ts>(ts)...};
}

// 使用
auto shape = make_shape(128, 64, 32);  // 三维shape
auto stride = make_stride(2048, 32, 1);
```

---

## 9. 总结

| 概念 | 说明 | 示例 |
|------|------|------|
| 函数模板 | 类型参数化的函数 | `template<typename T> T max(T a, T b)` |
| 类模板 | 类型参数化的类 | `template<typename T> class Stack` |
| 非类型参数 | 编译期常量作为模板参数 | `template<int N> class Array` |
| 模板模板参数 | 模板作为模板参数 | `template<template<typename> class C>` |
| 变参模板 | 接受任意数量参数 | `template<typename... Args>` |
| 隐式实例化 | 使用时自动实例化 | 编译器自动处理 |
| 显式实例化 | 强制在指定位置实例化 | `template class Stack<int>;` |

---

## 📚 延伸阅读

- [cppreference - Function template](https://en.cppreference.com/w/cpp/language/function_template)
- [cppreference - Class template](https://en.cppreference.com/w/cpp/language/class_template)
- [cppreference - Parameter pack](https://en.cppreference.com/w/cpp/language/parameter_pack)

